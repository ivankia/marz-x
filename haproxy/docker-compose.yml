services:
  haproxy:
    image: haproxy:2.8-alpine
    container_name: haproxy
    restart: always
    network_mode: host
    env_file: .env
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
